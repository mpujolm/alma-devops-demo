<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Customer_Price__c</fullName>
    <description>Calcula el precio final al cliente, sobre el precio por remesa, le aplica un incremento y lo divide por las unidades generadas en la remesa</description>
    <formula>IF(Batch_Production__c = 0, 0, (Batch_price__c * Batch_price_increase__c) / Batch_Production__c)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Total Unit Cost * Batch price increase</inlineHelpText>
    <label>Customer Price</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
